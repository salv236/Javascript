// function that returns the longest word 

function longestWord(word1, word2) {

    if(word1.length > word2.length || word2.length > word1.length) {
    return true;
    } return false;
}

document.write(longestWord("Salvatore", "Saia"));