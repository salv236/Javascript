// EXAMPLES

// doubles input value and returns it, multiple parametres can be passed
const doubler = (item) => item * 2;

//EXERCISE

"use strict";

const myConcat = (arr1, arr2) => arr1.concat(arr2); {

};
// test your code
console.log(myConcat([1, 2], [3, 4, 5]));